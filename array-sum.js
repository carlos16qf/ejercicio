function arraySumValues(array) {
    
    let number = 0;
    // noinspection JSUnusedAssignment
    for(let i = 0; i < array.length; i++ )
    {
         number += array[i];

    }
    return number
}
