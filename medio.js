const array = [1, 2, 7, 2, 4];
const array2 = [2, 3, 4, 54, 56, 43]

// si la longitud es par hay 2 número en medio
// si la cantidad de elementos es impar hay un número en medio
// ¿Cómo sabemos si la longitud es par o impar?

// contamos la cantidad de elementos y la divimos entre 2
// al hacer la división recibimos un valor
    // si el número tiene decimales la longitud es impar
    // si el número no tiene decimales la longitud es par

    // si al divivir la longitud me da como resultado un numero par
        // dividir la longitud
        // validar si el resultado es par
const m =array.length / 2;
if(Number.isInteger(m)){
    const result3 = Math.round(m);

    let result4 = array[result3];

} else {
    const result = array2[par];
const result2 = array2[par + 1];
const array_par = [result, result2]
}



const impar = array.length / 2 - 1; //  2.5 es par
const par = array2.length / 2 - 1
// averiguar el número de la mitad

const result3 = Math.round(m);

let result4 = array[result3];


console.log(result4)



const result = array2[par];
const result2 = array2[par + 1];
const array_par = [result, result2]

console.log(array_par)

    
